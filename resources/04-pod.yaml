apiVersion: v1
kind: Pod
metatadata:
  namespace: roboshop
  name: frontend
  labels:
    app: frontend
    proj: roboshop
    env: dev
spec:
  containers:
    - name: frontend
      image: nginx:latest