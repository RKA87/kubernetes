deployment:
  name: "nginx"
  replicas: 1
  container_name: "frontend"
  changeCause: "configuring frontend latest using helm"


image:
  # repository: ""
  versionTag: "latest"

service:
  port: 80
  containerPort: 80

configmap:
  name: "nginxconfig"