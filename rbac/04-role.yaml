apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  namespace: roboshop
  name: test-role
  labels:
    proj: roboshop
    env: dev
    deployment: kubernetes
rules:
  - apiGroups: [""]
    resources: ["pods", "statefulset", "deployment"]
    actions: ["get", "watch", "list"]