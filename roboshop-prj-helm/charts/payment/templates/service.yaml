apiVersion: v1
kind: Service
metadata:
  namespace: roboshop
  name: payment
  labels:
    proj: roboshop
    env: dev
    deployment: kubernetes
    app: payment
spec:
  selector:
    proj: roboshop
    env: dev
    deployment: kubernetes
    app: payment
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080