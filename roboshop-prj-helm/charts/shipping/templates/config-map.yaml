apiVersion: v1
kind: ConfigMap
metadata:
  namespace: roboshop
  name: shipping-configmap
  labels:
    proj: roboshop
    env: dev
    deployment: kubernetes
    tier: backend
data:
  CART_ENDPOINT: "cart:8080"
  DB_HOST: mysql
