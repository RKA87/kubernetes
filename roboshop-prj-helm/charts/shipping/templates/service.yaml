apiVersion: v1
kind: Service
metadata:
  namespace: roboshop
  name: shipping
  labels:
    proj: roboshop
    env: dev
    deployment: kubernetes
    app: shipping
spec:
  selector:
    proj: roboshop
    env: dev
    deployment: kubernetes
    app: shipping
  ports:
    - port: 8080
      targetPort: 8080