apiVersion: v1
kind: Service
metadata:
  namespace: roboshop
  name: cart
  labels:
    proj: roboshop
    env: dev
    deployment: kubernetes
    app: cart
spec:
  selector:
    proj: roboshop
    env: dev
    deployment: kubernetes
    app: cart
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080