apiVersion: v1
kind: Service
metadata:
  namespace: roboshop
  name: catalogue
  labels:
    proj: roboshop
    env: dev
    deployment: kubernetes
    app: catalogue
spec:
  selector:
    proj: roboshop
    env: dev
    deployment: kubernetes
    app: catalogue
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080