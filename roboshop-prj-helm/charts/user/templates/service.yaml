apiVersion: v1
kind: Service
metadata:
  namespace: roboshop
  name: user
  labels:
    proj: roboshop
    env: dev
    deployment: kubernetes
    app: user
spec:
  selector:
    proj: roboshop
    env: dev
    deployment: kubernetes
    app: user
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
